#include<stdio.h>
#include<math.h>
#include<stdlib.h>
#include<string.h>
#include<ctype.h>
int main()
{
    long long int no,arr[100];
    long long int a;
    scanf("%lld",&no);
    for(i=0;i<no;i++)
    {
        scanf("%lld",&arr[i]);
    }
    a=pow(2,no);
    for(i=0;i<no;i++)
    {
        for()
    }
    return 0;
}
