#include<bits/stdc++.h>
int main()
{
    int t;
    scanf("%d",&t);
    while(t--){
        long long int i,temp,n,sum=0,ar[50005];
        scanf("%lld",&n);
        for(i=0;i<n;i++){
            scanf("%lld",&ar[i]);
        }
        sort(ar[0],ar[n-1]);

    }
    return 0;
}
